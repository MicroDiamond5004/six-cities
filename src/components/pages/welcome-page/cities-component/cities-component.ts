import { amsterdamOffers } from '../../../../mocks/amsterdam-offers';
import { brusselsOffers } from '../../../../mocks/brussels-offers';
import { cologneOffers } from '../../../../mocks/cologne-offers';
import { dusseldorfOffers } from '../../../../mocks/dusseldorf-offers';
import { hamburgOffers } from '../../../../mocks/hamburg-offers';
import { parisOffers } from '../../../../mocks/paris-offers';


export const offersCityLocations = {
  Paris: parisOffers,
  Cologne: cologneOffers,
  Brussels: brusselsOffers,
  Amsterdam: amsterdamOffers,
  Hamburg: hamburgOffers,
  Dusseldorf: dusseldorfOffers,
};
